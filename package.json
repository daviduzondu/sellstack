{
  "name": "sellstack",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "nest": "pnpm --filter api exec nest g",
    "migrate:make": "pnpm -F api exec kysely migrate:make",
    "typebox": "pnpm -F api exec ts-node -r tsconfig-paths/register src/modules/db/scripts/generate-typebox-schemas.ts --project tsconfig.json",
    "migrate:latest": "pnpm -F api exec kysely migrate:latest && pnpm codegen && pnpm typebox",
    "codegen": "pnpm -F api exec kysely-codegen --runtime-enums=screaming-snake-case --dialect pg --out-file ./src/modules/db/generated/types.ts",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.9.18",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@11.6.0",
  "engines": {
    "node": ">=18"
  }
}
